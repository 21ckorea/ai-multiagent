# 멀티 AI Agent 오케스트레이터 — 시스템 구성도

> 작성일: 2026-07-26  
> 버전: 1.0.0 (릴리스)  
> 지원 OS: macOS / Windows (Electron 패키징)

---

## 1. 전체 레이어 구성

```mermaid
graph TB
    subgraph UI["🖥️ UI 레이어 (Electron / Browser)"]
        A1[프롬프트 관리<br>step CRUD / 편집]
        A2[워크플로우 관리<br>순서 편집 / 루프 설정]
        A3[접속 정보 설정<br>환경변수 / 비밀번호]
        A4[실행 & 모니터링<br>SSE 실시간 로그]
        A5[Gemini 연결<br>세션 상태]
    end

    subgraph SERVER["⚙️ API 서버 레이어 (Express.js)"]
        B1["REST API<br>/api/workflows<br>/api/steps<br>/api/settings"]
        B2[SSE 스트리밍<br>/api/events]
        B3[오케스트레이터<br>runWorkflow]
        B4[변수 치환 엔진<br>context 병합]
    end

    subgraph AGENTS["🤖 에이전트 레이어"]
        C1[gemini_agent<br>Gemini 웹 자동화]
        C2[gemini_image_agent<br>이미지 생성]
        C3[naver_blog_agent<br>네이버 포스팅]
        C4[tistory_blog_agent<br>티스토리 포스팅]
        C5[google_sheet_agent<br>구글 시트 연동]
    end

    subgraph LIBS["📦 라이브러리 레이어"]
        D1[gemini_compose<br>프롬프트 입력/응답]
        D2[naver_editor<br>스마트에디터 조작]
        D3[naver_image<br>이미지 삽입]
        D4[tistory_editor_inject<br>에디터 HTML 주입]
        D5[tistory_placeholder_gate<br>발행 레이어 제어]
        D6[kakao_login<br>카카오 로그인]
        D7[playwright_naver_publish<br>네이버 발행]
        D8[playwright_tistory_publish<br>티스토리 발행]
    end

    subgraph STORAGE["💾 스토리지 레이어"]
        E1[workflow_store<br>워크플로우 JSON]
        E2[settings.json<br>환경변수 / 접속정보]
        E3[Playwright Profile<br>퍼시스턴트 세션]
        E4[temp_images/<br>임시 이미지 파일]
    end

    subgraph EXTERNAL["🌐 외부 서비스"]
        F1[Gemini Web<br>gemini.google.com]
        F2[Naver Blog<br>blog.naver.com]
        F3[Tistory<br>tistory.com]
        F4[Google Sheets<br>sheets.google.com]
    end

    UI --> SERVER
    SERVER --> AGENTS
    AGENTS --> LIBS
    AGENTS --> STORAGE
    LIBS --> EXTERNAL
```

---

## 2. 디렉터리 구조

```mermaid
graph LR
    ROOT["📁 ai-multiagent/"]

    ROOT --> SRC["📁 src/"]
    ROOT --> CONFIG["📁 config/"]
    ROOT --> DATA["📁 data/"]
    ROOT --> PLANS["📁 plans/"]
    ROOT --> CRED["📁 credentials/"]

    SRC --> AGENT["📁 agent/"]
    SRC --> API["📁 api/"]
    SRC --> LIB["📁 lib/"]
    SRC --> STORAGE["📁 storage/"]
    SRC --> UI["📁 ui/"]

    AGENT --> AG1["gemini_agent.js<br>Gemini 웹 자동화 에이전트"]
    AGENT --> AG2["gemini_image_agent.js<br>이미지 생성 에이전트"]
    AGENT --> AG3["naver_blog_agent.js<br>네이버 블로그 포스팅"]
    AGENT --> AG4["tistory_blog_agent.js<br>티스토리 블로그 포스팅"]
    AGENT --> AG5["google_sheet_agent.js<br>구글 시트 연동"]
    AGENT --> AG6["session_manager.js<br>Gemini 세션 관리"]

    API --> AP1["server.js<br>Express REST API + SSE"]

    LIB --> LB1["gemini_compose.js<br>프롬프트 입력 / 응답 추출"]
    LIB --> LB2["naver_editor.js<br>스마트에디터 자동 입력"]
    LIB --> LB3["naver_image.js<br>이미지 클립보드 붙여넣기"]
    LIB --> LB4["tistory_editor_inject.js<br>에디터 HTML 직접 주입"]
    LIB --> LB5["tistory_placeholder_gate.js<br>발행 레이어 제어"]
    LIB --> LB6["kakao_login.js<br>카카오 로그인 자동화"]
    LIB --> LB7["playwright_naver_publish.js<br>네이버 발행 플로우"]
    LIB --> LB8["playwright_tistory_publish.js<br>티스토리 발행 플로우"]

    STORAGE --> ST1["workflow_store.js<br>워크플로우 / 스텝 CRUD"]

    UI --> UI1["index.html<br>메인 SPA UI"]
    UI --> UI2["app.js<br>UI 로직 / API 호출"]
    UI --> UI3["style.css<br>다크 테마 스타일"]
```

---

## 3. 플러그인 타입 목록

각 워크플로우 스텝은 아래 플러그인 타입 중 하나로 동작합니다.

| 플러그인 타입 | 설명 | 담당 에이전트 |
|---|---|---|
| `plugin_gemini` | Gemini 웹 UI에 프롬프트를 입력하고 응답을 받음 | `gemini_agent.js` |
| `plugin_gemini_image` | Gemini 이미지 생성 API로 이미지 파일 생성 | `gemini_image_agent.js` |
| `plugin_naver_publish` | 네이버 스마트에디터에 글을 작성하고 발행 | `naver_blog_agent.js` |
| `plugin_tistory_publish` | 티스토리 에디터에 HTML 글을 삽입하고 발행 | `tistory_blog_agent.js` |
| `plugin_google_sheet` | 구글 시트에서 데이터를 읽거나 씀 | `google_sheet_agent.js` |

---

## 4. 변수 치환 시스템

워크플로우 실행 시 프롬프트 내의 `{{변수명}}` 패턴은 아래 규칙으로 자동 치환됩니다.

```mermaid
graph LR
    A["프롬프트 텍스트<br>{{workflow_input}}<br>{{prev_output}}<br>{{step_xxx_output}}<br>{{blogId}}..."] --> B["변수 치환 엔진<br>server.js - context 병합"]
    C["워크플로우 입력값<br>workflow_input"] --> B
    D["이전 스텝 출력<br>prev_output"] --> B
    E["특정 스텝 출력<br>step_xxx_output"] --> B
    F["접속 정보 설정<br>settings.json"] --> B
    G["날짜<br>today"] --> B
    B --> H["실행 프롬프트<br>(치환 완료)"]
```

---

## 5. 기술 스택 요약

| 계층 | 기술 |
|---|---|
| 런타임 | Node.js 20 LTS |
| 데스크탑 패키징 | Electron + electron-builder |
| 브라우저 자동화 | Playwright (Chromium) |
| API 서버 | Express.js |
| UI | Vanilla HTML / CSS / JavaScript |
| 데이터 저장 | JSON 파일 (로컬) |
| 빌드 | electron-builder (Windows NSIS 인스톨러) |
